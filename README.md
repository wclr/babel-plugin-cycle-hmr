# babel-plugin-cycle-hmr

This is babel plugin for [cycle-hmr](https://github.com/whitecolor/cycle-hmr). Read the usage docs there.

Not supposed to be use on its own, use:
```
npm install cycle-hmr --save-dev
```
